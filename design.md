# GoPress 设计规范文档

> 面向 UI 设计师的设计参考文档  
> 项目：GoPress — 静态站点生成器（类 VitePress）  
> 最后更新：2026-06-02

---

## 1. 项目概述

GoPress 是一个将 Markdown 文件渲染为静态网站的工具，内置一套完整的主题系统。站点由**配置文件驱动**，设计师可通过修改配置控制导航、侧边栏、Logo、底部等元素。

### 1.1 两种页面类型

| 页面类型 | 触发条件 | 用途 |
|---------|---------|------|
| **首页 (Home)** | Markdown frontmatter 中 `layout: home` | 产品首页，包含 Hero 区 + 功能卡片 + 正文 |
| **文档页 (Doc)** | 其余所有普通页面 | 文档/文章内容展示，带侧边栏 |

### 1.2 运行模式

- **开发模式**：启动本地服务器，修改 Markdown 后浏览器自动刷新
- **构建模式**：生成纯静态 HTML/CSS/JS，可直接部署至任意 CDN

---

## 2. 全局布局架构

页面采用 **CSS Flexbox 纵向布局**，自上而下由 4 个区域组成：

```
┌──────────────────────────────────────┐
│  VPNav            顶部导航栏 (sticky) │  ← 固定 64px 高
├────────┬─────────────────────────────┤
│VPSidebar│  VPContent                  │
│ 侧边栏  │  内容区域 (flex-grow)        │
│ 272px  │                             │
│        │  ┌── 首页 ──────────────┐   │
│(仅文档页)│  │ · VPHero    (Hero区) │   │
│        │  │ · VPFeatures (特性卡片)│   │
│        │  │ · main      (正文)    │   │
│        │  └──────────────────────┘   │
│        │  ┌── 文档页 ────────────┐   │
│        │  │ · main      (正文)    │   │
│        │  │ · VPDocFooter(上下页) │   │
│        │  └──────────────────────┘   │
├────────┴─────────────────────────────┤
│  VPFooter         全站页脚            │  ← 玻璃拟态效果
└──────────────────────────────────────┘
```

- **首页**无侧边栏，内容区全宽
- **文档页**桌面端内容区左边距 272px（侧边栏宽度）

---

## 3. 设计系统

### 3.1 色彩体系

整体风格为**暖粉紫色调 (Warm Pink/Purple)**，柔和现代，支持明暗双主题。

#### 品牌色

| Token | 色值 | 用途 |
|-------|------|------|
| `--vp-c-brand` | `#D16BA5` | 主色：链接、按钮背景、激活态 |
| `--vp-c-brand-light` | `#E68FAC` | 主色浅色：按钮 hover |
| `--vp-c-brand-dark` | `#B94A8C` | 主色深色（预留） |

#### 亮色模式配色

| Token | 色值 | 用途 |
|-------|------|------|
| 页面背景 | `#FDF7F9` | 极浅粉白 |
| 次要背景 | `#F8F0F4` | 卡片、侧边栏、内联代码 |
| 主文字色 | `#4A3A42` | 正文、标题 |
| 次级文字色 | `#6B5A64` / 80% 透明度 | 描述文本、辅助信息 |
| 边框色 | `#E8DCE2` | 分割线、卡片边框 |
| 导航栏背景 | `#FDF7F9` | 同页面背景 |
| 侧边栏背景 | `#F8F0F4` | 同次要背景 |

#### 暗色模式配色

| Token | 色值 | 用途 |
|-------|------|------|
| 页面背景 | `#1A151B` | 深紫黑 |
| 次要背景 | `#141015` | 比主背景更深 |
| 主文字色 | `#F0E0E6` | 浅粉白 |
| 次级文字色 | `#D9C5CD` / 60% 透明度 | 辅助文字 |
| 边框色 | `#3A2E39` | 暗色边框 |
| 导航栏背景 | `#1A151B` | 同页面背景 |
| 侧边栏背景 | `#1A151B` | 同页面背景 |

#### 代码块

代码块不受主题影响，始终保持深色：

- 背景：`#1E1E20`（接近 GitHub Dark 风格）
- 文字：白色
- 圆角：8px

### 3.2 字体排版

#### 字体族

```css
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
             Roboto, Oxygen, Ubuntu, Cantarell, "Fira Sans",
             "Droid Sans", "Helvetica Neue", sans-serif;
```

使用系统原生字体栈，无外部字体加载。

#### 字号与字重层级

| 元素 | 字号 | 字重 | 备注 |
|------|------|------|------|
| Hero 主标题 `.name` | `3rem` (48px) | `700` | 字母间距 `-0.4px` |
| Hero 副标题 `.text` | `3rem` (48px) | `700` | 颜色为次级文字色 |
| Hero 标语 `.tagline` | `1.5rem` (24px) | `400` | 距上 24px，距下 48px |
| Features 标题 `.title` | `1.2rem` (19.2px) | `600` | |
| 文档页 h1-h4 | 默认继承 | 默认 | 上边距 1.5rem |
| 导航栏标题 | `1.3rem` (20.8px) | `600` | |
| 导航链接 | `0.9rem` (14.4px) | `500` | |
| 侧边栏一级节点 | `1rem` (16px) | `700` | 颜色同主文字色 |
| 侧边栏链接项 | `0.95rem` (15.2px) | `500` | 颜色为次级文字色 |
| 侧边栏分组标题 | `0.95rem` (15.2px) | `600` | |
| 上下页描述 | `11px` | `500` | |
| 上下页标题 | `14px` | `500` | 颜色为品牌色 |
| 页脚文字 | `0.9rem` (14.4px) | `400` | |
| 正文行高 | `1.6` | — | |
| Features 描述行高 | `1.5` | — | |

#### 内联代码

- 等宽字体：`monospace`
- 字号：`0.9em`（相对于父元素）
- 背景：次要背景色
- 内边距：`0.2em 0.4em`
- 圆角：`3px`

### 3.3 间距系统

| 场景 | 间距值 |
|------|--------|
| 导航栏高度 | `64px` |
| 导航栏左右内边距 | `32px` |
| 导航栏菜单间距 | `1rem` (16px) |
| 侧边栏宽度 | `272px` |
| 侧边栏内边距 | `32px` |
| 侧边栏根组间距 | `24px` |
| 侧边栏项间距 | `6px` |
| 侧边栏嵌套组缩进 | `16px` (左边距) |
| 内容区顶部间距 | `24px` |
| 文档页内边距 | `32px 24px` |
| 文档页最大宽度 | `768px` |
| Hero 区内边距 | `48px 24px` |
| Features 网格间距 | `24px` |
| Features 卡片内边距 | `24px` |
| Features 最大宽度 | `1152px` |
| 代码块内边距 | `1rem` |
| 按钮圆角 | `20px` |
| 按钮内边距 | `8px 16px` |
| Features 卡片圆角 | `12px` |
| 页脚内边距 | `32px 24px` |
| VPDocFooter 上边距 | `64px` |
| 上下页链接内边距 | `11px 16px 13px` |
| 上下页链接圆角 | `8px` |

### 3.4 动效与过渡

| 元素 | 动效 | 时长 | 缓动 |
|------|------|------|------|
| 背景色切换 (亮/暗) | `background-color` | `100ms` | — |
| 侧边栏滑入/滑出 | `transform: translateX` | `150ms` | `ease` |
| 侧边栏折叠箭头旋转 | `transform: rotate` | `250ms` | `ease` |
| 侧边栏箭头颜色 | `color` | `150ms` | — |
| 侧边栏链接颜色 | `color` | `150ms` | — |
| 按钮颜色变化 | `color, background-color` | `150ms` | — |
| 社交图标颜色 | `color` | `150ms` | — |
| 上下页链接边框 | `border-color` | `250ms` | — |
| 上下页链接标题色 | `color` | `250ms` | — |
| SPA 导航 | 异步 fetch + DOM 替换 | 即时 | — |

---

## 4. 组件规范

### 4.1 顶部导航栏 (VPNav)

**结构**：左侧站点标题/Logo + 右侧导航菜单 + 主题切换 + 社交链接

```
┌────────────────────────────────────────────────────────────┐
│ [Logo] My GoPress    Home  Guide  IceTest  [☀️] [GH]  [☰] │
└────────────────────────────────────────────────────────────┘
```

| 特性 | 说明 |
|------|------|
| 定位 | `position: sticky; top: 0; z-index: 10` |
| 高度 | 64px |
| 背景 | 半透明导航栏背景色 + 底部 1px 边框 |
| Logo | 支持明暗双主题图片，高 24px，右边距 8px |
| 暗色模式按钮 | ☀️ / 🌙 emoji 切换，亮色模式仅显示🌙，暗色模式仅显示☀️ |
| 社交链接 | 可配置，GitHub 图标通过 CSS mask 渲染 SVG |
| 汉堡菜单 | ☰ 字符，仅在文档页移动端显示（< 768px） |
| 导航菜单 | 移动端隐藏，桌面端 `flex` 排列，间距 1rem |

#### 交互状态
- 导航链接 hover → 颜色变为品牌色
- 社交链接 hover → 颜色变为主文字色
- 主题切换 → 立即切换 + 存入 localStorage

### 4.2 侧边栏 (VPSidebar)

**结构**：分组列表，支持可折叠节点和多级嵌套

```
┌────────────────────┐
│ 导航               │  ← 分组标题，加粗显示
│   导航介绍          │  ← 可点击链接
│                    │
│ 一级菜单 ▸          │  ← 可折叠组，展开状态
│   二级菜单首页      │
│   二级次页面        │
│   二级菜单 ▸        │  ← 二级可折叠组，折叠状态
│                    │
│ 历史记录            │
│   折叠状态持久化    │
│   侧边栏状态重构    │
│   侧边栏防闪烁      │
│   ...              │
│                    │
│ IceTest →          │  ← 直接链接
└────────────────────┘
```

| 特性 | 说明 |
|------|------|
| 定位 | `position: fixed; top: 64px; bottom: 0; left: 0` |
| 宽度 | 272px |
| z-index | 20（高于导航栏） |
| 背景 | 侧边栏背景色 |
| 滚动 | 内容超长时内部滚动 (`overflow-y: auto`) |
| 移动端 | 默认隐藏 (`translateX(-100%)`)，点击汉堡菜单滑出 |
| 桌面端 | 固定显示，右侧有 1px 分隔线 |
| 嵌套缩进 | 每组左边距 16px + 左侧 1px 边框线 |
| 折叠箭头 | SVG chevron-right，展开状态旋转 90°，折叠状态 0° |
| 状态持久化 | 折叠状态存入 localStorage，跨页面保持 |

#### 节点类型

| 类型 | 特征 | 视觉 |
|------|------|------|
| **分组标题** | 有 `text`，无 `link`，有子项 | 加粗 600，颜色同主文字 |
| **直接链接** | 有 `text`，有 `link`，无子项 | 字重 500，颜色为次级文字，hover 变品牌色 |
| **可折叠组** | 有 `text`，有子项，右侧箭头图标 | `collapsed` 控制折叠状态 |

### 4.3 按钮 (VPButton)

两种主题风格，通过 `.theme` 字段配置：

#### Brand 按钮（主操作）
- 背景：品牌色 `#D16BA5`
- 文字：白色
- hover：背景变为 `#E68FAC`
- 用途：Hero 区主 CTA

#### Alt 按钮（次操作）
- 背景：次要背景色 `#F8F0F4`
- 文字：主文字色 `#4A3A42`
- hover：背景变为边框色 `#E8DCE2`
- 用途：Hero 区副 CTA

通用样式：
- 内联块级元素，圆角 20px，内边距 8px 16px
- 字重 600
- 过渡动画 150ms

### 4.4 Hero 区 (VPHero) — 仅首页

```
┌──────────────────────────────────────┐
│                                      │
│          GoPress                      │  ← .name 大标题 3rem
│          Simple, Powerful            │  ← .text 副标题 3rem
│          简洁强大的静态站点生成器       │  ← .tagline 标语 1.5rem
│                                      │
│        [开始使用]  [GitHub]           │  ← .actions 按钮组
│                                      │
└──────────────────────────────────────┘
```

| 特性 | 说明 |
|------|------|
| 对齐 | 文本居中 (`text-align: center`) |
| 内边距 | 48px 24px |
| 按钮组 | `flex` 水平排列，间距 16px，居中 |
| 可选元素 | `.name` `.text` `.tagline` `.actions` 均可单独配置 |

### 4.5 功能卡片区 (VPFeatures) — 仅首页

响应式网格布局：

```
┌──────────┐ ┌──────────┐ ┌──────────┐
│  特性 A  │ │  特性 B  │ │  特性 C  │
│  描述... │ │  描述... │ │  描述... │
└──────────┘ └──────────┘ └──────────┘
```

| 特性 | 说明 |
|------|------|
| 布局 | CSS Grid，`auto-fit` + 最小 280px 列宽 |
| 间距 | 24px |
| 最大宽度 | 1152px，水平居中 |
| 卡片背景 | 次要背景色 |
| 卡片圆角 | 12px |
| 卡片内边距 | 24px |
| 卡片标题 | 1.2rem，字重 600，下边距 8px |
| 卡片描述 | 次级文字色，行高 1.5 |

### 4.6 文档底部导航 (VPDocFooter)

文档页底部自动生成上下页导航：

```
┌───────────────────────────────────────────┐
│ ┌───────────────┐   ┌───────────────────┐ │
│ │ 上一页         │   │            下一页  │ │
│ │ 导航介绍  →    │   │   ←  二级菜单首页 │ │
│ └───────────────┘   └───────────────────┘ │
└───────────────────────────────────────────┘
```

| 特性 | 说明 |
|------|------|
| 距正文 | 64px，顶部 1px 分割线 |
| 卡片样式 | 边框 1px，圆角 8px，内边距 11px 16px 13px |
| hover | 边框变品牌色，标题变品牌色 |
| "上一页"左对齐 | "下一页"右对齐 (`text-align: right`) |
| 描述文字 | 11px，字重 500，次级文字色 |
| 标题文字 | 14px，字重 500，品牌色 |

### 4.7 页脚 (VPFooter)

采用**玻璃拟态 (Glassmorphism)** 设计风格。

```
┌──────────────────────────────────────────┐
│  Released under the MIT License.         │  ← 带品牌色下划线
│  Copyright © 2026 GoPress Contributors   │  ← 0.8 透明度
└──────────────────────────────────────────┘
```

| 特性 | 说明 |
|------|------|
| 背景 | 半透明白色 `rgba(255,255,255,0.03)` |
| 模糊 | `backdrop-filter: blur(10px)` |
| 叠加层 | 伪元素渐变：蓝 → 紫 → 粉，10% 透明度 |
| 边框 | 顶部半透明白色线 |
| 文字 | 次级文字色，0.9rem |
| message 底部 | 2px 品牌色装饰线，60% 透明度 |
| 首页 | 全宽；文档页桌面端左边距 272px |
| z-index | 容器层级 666 |

---

## 5. 响应式设计

单一断点策略：**768px**

### 5.1 移动端 (`< 768px`)

- 顶部导航菜单**隐藏**，仅显示标题、主题切换、社交链接
- 文档页显示**汉堡菜单按钮** ☰
- 侧边栏**默认隐藏**（`translateX(-100%)`），点击汉堡菜单滑入
- 侧边栏打开后覆盖内容区（`z-index: 20`）
- 内容区**全宽显示**
- 页脚**全宽**
- Features 网格自动缩为单列

### 5.2 桌面端 (`>= 768px`)

- 顶部导航菜单**显示**（flex 水平排列）
- 汉堡菜单按钮**隐藏**
- 侧边栏**固定显示**（`translateX(0)`），右侧有 1px 分隔线
- 文档页内容区**左边距 272px**
- 页脚**左边距 272px**（非首页）
- Features 网格多列自适应

---

## 6. 交互说明

### 6.1 SPA 客户端导航

- 站内链接点击**不刷新整页**，通过 JavaScript fetch 获取新页面 HTML
- 替换 `#Layout` 容器内容，更新标题和历史记录
- 浏览器前进/后退同样走 SPA 路由
- 导航后自动滚动到顶部、关闭移动端侧边栏、恢复侧边栏折叠状态
- **外部链接**正常跳转

### 6.2 暗色模式

- 页面加载前通过内联脚本同步设置 `html.dark` 类，**避免闪烁**
- 优先级：localStorage 存储 > 系统偏好 (`prefers-color-scheme`)
- 切换后立即存入 localStorage
- 主题切换按钮根据当前主题显示对应图标

### 6.3 侧边栏折叠

- 点击箭头折叠/展开，箭头旋转 90° 动画
- 折叠状态存入 localStorage，**跨页面保持**
- 页面加载时通过 MutationObserver **提前恢复**折叠状态，消除闪烁
- SPA 导航后自动恢复

### 6.4 可主题化图片

- 同时提供亮色和暗色两张图片
- `.light-only`：仅亮色模式可见
- `.dark-only`：仅暗色模式可见

---

## 7. 配置文件驱动

以下元素均可通过 `config.json` 配置，无需修改代码：

| 配置项 | 路径 | 说明 |
|--------|------|------|
| 站点标题 | `.title` | 显示在浏览器标签页 + 导航栏 |
| Logo | `.themeConfig.logo.light / .dark` | 亮/暗双主题 Logo URL |
| Logo 替代文本 | `.themeConfig.logo.alt` | 图片 alt 属性 |
| 导航菜单 | `.themeConfig.nav[]` | 每个项含 `text` + `link` |
| 侧边栏 | `.themeConfig.sidebar` | 以路径前缀为 key 的对象 |
| 社交链接 | `.themeConfig.socialLinks[]` | 含 `icon` 名 + `link` |
| 页脚消息 | `.themeConfig.footer.message` | 支持 HTML（如链接） |
| 页脚版权 | `.themeConfig.footer.copyright` | 版权声明文本 |
| 首页 Hero | Markdown frontmatter | `hero.name` `hero.text` `hero.tagline` `hero.actions` |
| 首页 Features | Markdown frontmatter | `features[].title` `features[].details` |
| 语言 | `.lang` | `<html lang>` 属性 |

---

## 8. 设计检查清单

在新页面设计时，请确认以下要点：

- [ ] **亮色/暗色模式**下所有内容是否清晰可读？
- [ ] 移动端（< 768px）侧边栏滑出后是否遮挡内容？
- [ ] 代码块在明暗主题下是否保持一致？
- [ ] 若有 Logo，是否提供了亮色和暗色两个版本？
- [ ] 长文档页面侧边栏是否可滚动？
- [ ] 上下页导航是否在所有文档页底部正常出现？
- [ ] 按钮颜色是否符合品牌色体系？
- [ ] 功能卡片在移动端是否自动堆叠？
- [ ] SPA 导航后页面滚动位置是否正确？
- [ ] 侧边栏折叠状态跨页面是否保持一致？

---

## 9. 附录：CSS 变量速查表

```css
/* === 品牌色（不随主题变化）=== */
--vp-c-brand:       #d16ba5;
--vp-c-brand-light: #e68fac;
--vp-c-brand-dark:  #b94a8c;

/* === 亮色主题 === */
--vp-c-bg:            #fdf7f9;  /* 页面背景 */
--vp-c-bg-alt:        #f8f0f4;  /* 次要背景 */
--vp-c-text:          #4a3a42;  /* 主文字 */
--vp-c-text-2:        #6b5a64cc; /* 次级文字 */
--vp-c-border:        #e8dce2;  /* 边框 */
--vp-c-divider:       #e8dce2;  /* 分割线 */
--vp-nav-bg-color:    #fdf7f9;  /* 导航栏背景 */
--vp-sidebar-bg-color:#f8f0f4;  /* 侧边栏背景 */

/* === 暗色主题 (html.dark) === */
--vp-c-bg:            #1a151b;
--vp-c-bg-alt:        #141015;
--vp-c-text:          #f0e0e6;
--vp-c-text-2:        #d9c5cd99;
--vp-c-border:        #3a2e39;
--vp-c-divider:       #3a2e39;
--vp-nav-bg-color:    #1a151b;
--vp-sidebar-bg-color:#1a151b;
```

---

*如需调整主题色系，修改 `theme.css` 中 CSS 变量即可全局生效。*
